from setuptools import setup, find_packages

setup(
    name="ESQB",
    version="0.1.0",
    description="A clean-room, original Elasticsearch query builder for Python.",
    author="Shahram Saghaei",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
