from setuptools import setup, find_packages

setup(
    name="esqb",
    version="0.1.0",
    description="An Elasticsearch query builder for Python.",
    author="Shahram Saghaei",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
